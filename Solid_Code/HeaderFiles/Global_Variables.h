#define MIN(a,b) (((a)<(b))?(a):(b))
#define MAX(a,b) (((a)>(b))?(a):(b))

// Bools
bool print_sf_points = false;
bool print_mesh_points = false;
bool print_mesh_lines = false;

double PI = 3.14159265359;